# Environment Configuration Example
# Copy this file to .env and update with your values
# DO NOT commit .env to version control

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASS=your_database_password

# JWT Configuration
# Use a strong, random secret key (minimum 32 characters)
ACCESS_SECRET=your-very-secret-jwt-key-change-this-to-random-string

# Application Configuration
APP_ENV=production
# Set to 'development' for debugging, 'production' for live

# CORS Configuration
# Use * for all origins, or comma-separated list: https://example.com,https://app.example.com
CORS_ALLOWED_ORIGINS=*

# Note: On shared hosting, you may need to set these in cPanel
# or your hosting provider's environment variable settings
